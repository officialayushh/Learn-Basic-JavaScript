// find an array 
let names = [1,2,4,"Ayush",undefined]; // Anything you give here int,float,char,string.
console.log(names[1]); // For finding an element at current index.

// Also type this as a format.
let ayush = [6,8,2,4,9,"Banana"];
console.log(ayush);
console.log(ayush.length);
console.log(ayush.sort()); // Decrease to increase;
console.log(ayush.push("Extra","ab"));  // Length m add kardega extra element;
